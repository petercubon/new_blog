{block content}

<div class="container">
    <div class="row">
        <div class="col-12 col-sm-2 text-center">
            <img src="{$baseUrl}/img/avatar.svg" class="img-fluid">
            {$user->identity->name} {$user->identity->surname}
            {if $status == 0}
                {control emailVerificationForm}
            {/if}
        </div>
        {control userComment}
    </div>
</div>

{/block}